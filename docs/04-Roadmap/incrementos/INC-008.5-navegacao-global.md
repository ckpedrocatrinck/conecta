# INC-008.5 — Navegação global (bottom nav + header admin)

**Status:** ✅ Concluído
**Fase:** transversal (entre 3 e 4) — fundação de navegação, análoga ao INC-003.5 de design
**Depende de:** INC-008 (feed) mergeado; ADR-009 aceito
**ADRs relevantes:** 009 (navegação por papel), 002 (mobile-first), 003 (papéis)
**Resolve:** DP-10

## Por que este INC existe
Até aqui todas as telas são acessíveis só por link direto ou URL digitada à mão (o painel admin só aparece em `/admin`). Este INC implementa a navegação global do ADR-009 — bottom nav para colaborador, menu administrativo no header para admin — tornando o produto navegável sem decorar URLs. É fundação transversal: toca o layout, não uma feature isolada.

## Escopo

### 1. Bottom navigation (colaborador, e visível a todos os papéis)
- Popular o componente `BottomNav` (criado no INC-003.5) com os itens reais: **Início** (`/`), **Comunicados** (`/comunicados`), **Perfil** (`/perfil`).
- Item ativo destacado em `--primary` (design-system §4); inativo em `--muted-foreground`; ícones lucide.
- **Badge de pendência** no item Comunicados: contador de comunicados aguardando ciência do usuário logado — reusar `countPendingAcksForUser` (INC-005), NÃO recalcular. Some quando zero.
- Fixo no rodapé, alvos ≥48px, seguro em 360px e em safe-area de iOS (padding bottom).
- Aparece para employee, manager e admin (todos veem o produto de colaborador).

### 2. Header com menu administrativo (só admin)
- Header no topo, visível apenas quando `session.role === "admin"`, com acesso a: **Comunicados** (`/admin/comunicados`), **Posts** (`/admin/posts`), **Colaboradores** (`/admin/colaboradores`), **Filiais** (`/admin/filiais`), **Pendências** (`/pendencias`), **Auditoria** (`/admin/auditoria`).
- Em telas estreitas (360px) o menu admin colapsa de forma limpa (ex.: menu recolhível) — mas SEM reproduzir o hambúrguer-de-tudo da portal legado; é só o conjunto administrativo, organizado.
- Deixa de existir a necessidade de digitar `/admin` na mão.

### 3. Manager
- Bottom nav igual + um acesso a **Pendências** (`/pendencias`). Sem o menu admin completo. (Código dormente no piloto, mas correto por papel.)

### 4. Integração no layout
- A navegação é resolvida no layout raiz a partir do `role` da sessão — o que cada usuário vê depende do papel, aplicado no servidor (não esconder no cliente algo que o servidor serviria).
- Rotas administrativas continuam protegidas pelos guards existentes (`requireAdmin`/`requireAdminOrManager`) — a navegação é conveniência de UI, NÃO substitui a autorização de rota. Um employee que digite `/admin` continua bloqueado como antes.
- Remover/ajustar os links soltos "de emergência" que INCs anteriores puseram nas home (ex.: link "Painel de pendências" solto em `page.tsx`) agora que há navegação de verdade — sem quebrar nada.
- Adicionar o link **Posts** que faltava no painel admin (observado no QA do INC-008).

## Fora do escopo
- Vagas no bottom nav (entra no INC-011).
- Central de notificações dedicada (depende de mais decisão de produto; notificações in-app seguem na home).
- Redesenho de qualquer tela interna — só a navegação que as amarra.

## Critérios de aceite
- [x] Colaborador vê bottom nav com Início/Comunicados/Perfil; item ativo destacado; tudo acessível sem digitar URL — `src/components/nav/app-bottom-nav.tsx` + `src/components/ui/bottom-nav.tsx`.
- [x] Badge de pendência aparece no item Comunicados quando há pendência e some quando zera (bate com `countPendingAcksForUser`) — `getCachedPendingAckCount` (`src/lib/announcements/list-for-user.ts`), testado em `tests/integration/pending-count-badge.test.ts`.
- [x] Admin vê o bottom nav + menu administrativo no header com as 6 telas; nenhuma exige URL decorada — `src/components/admin/admin-header-nav.tsx`.
- [x] Employee que force `/admin/*` por URL continua bloqueado (navegação não afrouxa autorização) — guards inalterados, testado em `tests/integration/admin-guard-authorization.test.ts`.
- [x] Manager vê bottom nav + acesso a Pendências, sem o menu admin completo — variante de um link em `admin-header-nav.tsx`.
- [x] Tudo usável em 360px, sem scroll horizontal, alvos ≥48px, respeitando safe-area — `viewport-fit=cover` (`src/app/layout.tsx`) + `env(safe-area-inset-bottom)` no `BottomNav`.
- [x] Navegação resolvida por papel no servidor — `src/app/(app)/layout.tsx` lê `requireOnboardedSession().role`.

## Registro de conclusão

- **Data:** 2026-07-13
- **Branch:** `inc-008.5-navegacao-global`
- **Commit de merge (--no-ff):** `b1f756a`
- **ADR-009 (navegação por papel):** implementado nesta entrega — bottom nav
  (todos os papéis) + header admin (só admin) + acesso simplificado a
  Pendências (manager), navegação resolvida no servidor a partir do `role`
  da sessão. **Resolve o DP-10** (ver `docs/05-Decisoes-Pendentes.md`).
- **Decisões registradas:**
  - Route group `src/app/(app)/` agrupa as telas de produto (home,
    comunicados, perfil, pendências, admin) sob um único layout de navegação,
    sem alterar nenhuma URL; login/403/troca de senha/aviso de privacidade
    ficaram fora do grupo, sem nav.
  - `getActiveSession` e o novo `getCachedPendingAckCount` envolvidos em
    `cache()` (React) para deduplicar a consulta de sessão e de pendência
    entre o layout e a página em uma mesma navegação (sem N+1).
  - Header admin usa `<details>` semântico para colapsar em telas estreitas
    (sem JS/estado client) — mantém as 6 telas administrativas isoladas de
    qualquer outro menu, por design do ADR-009.
  - Link de Pendências do manager entra na mesma posição do header (variante
    de um único link), decisão de organização não 100% literal no INC,
    aprovada por Pedro no plano.
  - Corrigida referência cruzada equivocada no próprio ADR-009 ("ADR-003
    (papéis)" → papéis definidos no schema `UserRole` + guards), autorizado
    por Pedro durante a revisão do plano.
- **CI:** lint + typecheck + testes verdes antes do merge (18 arquivos, 103
  testes, incluindo os 2 novos deste INC).
- **Pendências criadas:** polimento visual do shell de navegação (header
  admin, identidade/logout no header, atalho "voltar ao início") registrado
  como débito de produto, agendado entre INC-012 e INC-013 — ver
  `docs/05-Decisoes-Pendentes.md`.
