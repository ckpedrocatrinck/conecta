# Copie este arquivo para .env e preencha os valores localmente.
# Nunca commitar .env (já está no .gitignore) nem colar segredo real no chat.
#
# Nenhuma variável de ambiente é necessária ainda no INC-001 (sem banco, sem auth).
# Novas variáveis introduzidas em INCs futuros (DATABASE_URL, AUTH_SECRET,
# CPF_HASH_PEPPER, VAPID keys, credenciais do R2 etc.) devem ser documentadas
# aqui, com comentário explicando o propósito de cada uma.
